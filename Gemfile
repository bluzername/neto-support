source "https://rubygems.org"

# Same gem set GitHub Pages uses to build the site, so `bundle exec jekyll serve`
# matches production. See https://pages.github.com/versions/
gem "github-pages", group: :jekyll_plugins
